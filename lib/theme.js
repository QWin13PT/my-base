/**
 * Theme Configuration
 * Color palette 
 */

export const colors = {
    dark: '#0F172A',      // Deep slate (near black) - for backgrounds, main text (replaces #000000 for better contrast/softness on dark modes)
    primary: '#0052FF',    // Base/Coinbase Blue - main accent, CTAs, highlights (trustworthy and energetic)
    accent: '#00D1FF',     // Bright cyan/blue accent - secondary highlights, links, interactive elements (complements the blue without clashing)
    light: '#F8FAFC',      // Soft light gray/white - cards, surfaces, backgrounds (clean and modern, high readability)
  };
  
  